This repo contains the self-study notebooks for Dive into Deep Learning.

To get started, create a new conda environment and install d2l, torch, and torchvision. Then run ```jupyter notebook``` from the CLI to get started.

1. `conda create --name d2l python=3.9 -y`
2. `conda activate d2l`
